MEXC Exhaustion Scanner + Multi-Slot Futures Trader v1.3.57

Deploy the entire project/ZIP together.

v1.3.57 IMPROVES OPPORTUNITY RECALL WITHOUT LOOSENING ENTRY CONFIRMATION
- Armed Runner V1 remembers a legitimate unconfirmed pump for 48h after the latest tracked peak/detection.
- Current run_score / 72h return may fade during that memory window; the short still requires exhaustion structure, structural break, failed retest, then Daily-Core + Persistence V2 admission.
- Confirmed episodes keep the existing +5% new-high re-arm and add a 48h time-based re-arm when the symbol independently qualifies again.
- Fixes the contradictory worker gate that previously rejected the classifier's intended late-prior-runner exception below STATE_MIN_RUN_SCORE.
- Adds lifecycle telemetry: new-high/time re-arms, confirmed locks, armed keeps/exhaustion/expiry, late-prior admissions.

CURRENT TRADER ALLOCATION
- STANDARD + HIGH_RISK
- 6 generic slots
- 8.333333333333% current equity per slot
- 50% max aggregate exposure
- 5 STANDARD slots + 1 HIGH_RISK slot
- TP +5%
- catastrophic SL -75%
- 1x cross
- one open position per symbol

Existing open positions are NOT resized. Keep the existing TRADER_PAPER_RUN_ID if you do not want deployment to archive/reset the current paper book.

Required/expected Render values:
Scanner:
  SUBSCRIBER_SIGNAL_STRATEGY=tp5_sl75_daily_core_persistence_skip_v2
  ARMED_RUNNER_MEMORY_HOURS=48
  CONFIRMED_REARM_HOURS=48
  REARM_NEW_HIGH_PCT=0.05
  EPISODE_MAX_AGE_HOURS=240

Trader:
  TRADER_EXECUTION_STRATEGY=tp5_sl75_daily_core_persistence_skip_v2
  TRADER_MAX_OPEN_POSITIONS=6
  TRADER_SLOT_ALLOCATION_PCT=8.333333333333
  TRADER_MAX_TOTAL_EXPOSURE_PCT=50
  TRADER_TP5_TARGET_PCT=5
  TRADER_CATASTROPHIC_STOP_PCT=75
  TRADER_ALLOWED_RISK_TIERS=STANDARD,HIGH_RISK

No new database migration is required.

Verification completed for this package:
  pytest -q  -> 278 passed
  python scripts/verify_project.py -> PROJECT VERIFICATION PASSED

Post-deploy checks:
  python -m app.trader_status
  python -m app.report_now
  python -m app.research_analytics_now

Expected scanner evaluation summary now includes fields such as:
  rearm_new_high=...
  rearm_timeout=...
  confirmed_locked=...
  armed_kept=...
  armed_exhaustion=...
  armed_expired=...
  late_prior_admissions=...
