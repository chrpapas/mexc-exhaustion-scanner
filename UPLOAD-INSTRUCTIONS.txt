MEXC Exhaustion Scanner + Multi-Slot Futures Trader v1.3.15

Deploy the entire project/ZIP together. v1.3.15 is a REPORTING release; frozen TP5 trader execution is unchanged.

Public Discord strategy choices:
- TP5 Frequent: STANDARD + HIGH_RISK, full +5% exit.
- TP20 High Risk: HIGH_RISK only, +20% target or 4-day timeout. Metrics use the strict paired 10-day cohort used in research.
- 7D Swing: STANDARD only, fixed 7-day hold.

All three show decision-grade W/L and return metrics where meaningful. EXTREME_RISK remains hard-suppressed. Exploratory TP1/TP2/hybrids/EntryGate/token-regime/feature sweeps remain internal.

No new DB migration. Latest remains 015_tp5_trader_runs.sql.

Frozen TP5_V1 paper strategy remains:
- STANDARD + HIGH_RISK only
- 6 generic slots
- 5% current equity per slot
- 30% aggregate cap
- 1x cross
- immediate entry; one open position per symbol
- full exit at +5% favorable short return

Reports:
python -m app.report_now
python -m app.research_analytics_now
python -m app.signal_ledger_now

Verification:
pytest -q
python scripts/verify_project.py
python -m app.trader_status
