MEXC Exhaustion Scanner + STANDARD Short Trader v1.1.5

Full deployment:
- Upload/deploy the entire repository.
- Render defines two workers sharing the same PostgreSQL database:
  1) mexc-exhaustion-scanner
  2) mexc-standard-short-trader
- Migration 011_strategy_threshold_matrix.sql is automatic.
- Performance board now includes a strategy viability matrix for STANDARD vs HIGH+EXTREME.
- Strategy rows: +20% target, 1D profitable, 2D profitable, 3D profitable, 7D profitable.
- Adverse thresholds: -100%, -200%, -300%, -400%.
- Average/summed profit is highlighted only for cells with 100% observed win rate.
- Trader execution behavior is unchanged from v1.1.4.
- Use `python -m app.report_now` in the scanner shell to trigger stats.
- Use `python -m app.trader_status` in the trader shell to inspect trader state.
