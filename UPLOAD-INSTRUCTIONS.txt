MEXC Exhaustion Scanner + Multi-Slot Futures Trader v1.3.9

Deploy the entire project/ZIP together. v1.3.9 is a RESEARCH-ONLY token-behaviour release; TP5 trader execution is unchanged from v1.3.6.

v1.3.9 retains the v1.3.8 research-path SQL timeout hotfix on larger datasets. The one-off analytics command no longer aborts if the optional path catch-up times out; it reports from the paths already persisted.

Frozen TP5_V1 paper strategy (unchanged):
- STANDARD + HIGH_RISK; EXTREME excluded
- 6 generic slots
- 5% of current equity per slot
- 30% aggregate exposure cap
- 1x cross
- immediate entry; one open position per symbol
- full exit at +5% favorable short return
- no conventional tight stop; adverse telemetry retained

Clean paper run (unchanged):
TRADER_EXECUTION_STRATEGY=tp5_v1
TRADER_PAPER_RUN_ID=tp5_v1
PAPER_STARTING_EQUITY_USDT=2000

Token-behaviour research in v1.3.9:
- 90d of completed pre-signal 4h token/BTC history
- no post-signal/look-ahead data in classification
- REGIME_FOLLOWER / MIXED / EPISODIC / INSUFFICIENT research labels
- TP5-All vs no-regime-followers vs episodic-only vs same-bar episodic-priority shadow replays
- capital-time metrics: slot-days, return/slot-day, releases/day, time-based idle capacity, avg/p95 exposure
- no scanner/trader rule changes
- no new DB migration

The scanner automatically backfills missing 4h left-edge history for stored research signal symbols. Keep:
RESEARCH_LOGGING_ENABLED=true
RESEARCH_PATH_HORIZON_HOURS=336
RESEARCH_REGIME_HISTORY_POLL_SECONDS=21600

On-demand research report:
python -m app.research_analytics_now

The Discord report includes a Token Behaviour • Regime Dependency card and attaches:
research-token-regime-YYYY-MM-DD.csv

If the behaviour card initially reports insufficient history, check scanner logs for:
Research regime-history sync complete
and rerun the on-demand research command after coverage exists.

Live mode later still ignores PAPER_STARTING_EQUITY_USDT and reads actual MEXC Futures USDT equity/available balance. Before any live switch run:
python -m app.trader_preflight

Verification before deployment:
pytest -q
python scripts/verify_project.py
python -m app.trader_status   # after deployment, from Render Shell

Latest migration remains 015_tp5_trader_runs.sql.
