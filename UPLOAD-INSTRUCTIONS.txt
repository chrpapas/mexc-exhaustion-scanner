MEXC Exhaustion Scanner + Multi-Slot Short Trader v1.3.5

Deploy the entire project directory/ZIP contents together.

v1.3.5 is a research-only extension. Live trader behavior remains the v1.3.1 strategy:
- 6 total slots; 5 STANDARD + 1 HIGH_RISK
- 20% aggregate initial notional exposure; ~3.3333% per slot by default
- STANDARD: fixed 7-day hold; +20% is telemetry only
- HIGH_RISK: close at +20% or 4-day timeout
- Entry Quality / Continuation Risk / EntryGate-v1 remain shadow-only
- Existing pre-v1.3 positions keep their persisted legacy exit strategy

New v1.3.5 research outputs:
- Calendar Throughput board comparing Current vs TP5 on all observed signals, including immature/open positions
- Signal utilization, capacity misses, entries/day, releases/day, marked return, MTM DD and median hold
- True latest 30-day empty-book replay after 30 calendar days of history; no short-window monthly return extrapolation
- Strategy CSV rows for observed calendar throughput and the latest true 30-day window
- Existing v1.3.4 prospective monitoring, v1.3.3 MTM/EntryGate, OOS freeze and v1.3.2 TP5 risk analytics remain intact
- No new MEXC requests and no database migration

Render trader variables to verify (unchanged):
TRADER_MAX_OPEN_POSITIONS=6
TRADER_MAX_TOTAL_EXPOSURE_PCT=20
TRADER_MAX_STANDARD_POSITIONS=5
TRADER_MAX_HIGH_RISK_POSITIONS=1
TRADER_STANDARD_HOLD_DAYS=7
TRADER_HIGH_RISK_TIMEOUT_DAYS=4
TRADER_PROFIT_TARGET_PCT=20
TRADER_LEVERAGE=1
TRADER_MARGIN_MODE=cross

Scanner research:
RESEARCH_PATH_HORIZON_HOURS=336
python -m app.research_analytics_now
python -m app.signal_ledger_now

Migration 014_tier_exit_strategy.sql remains the latest migration and is applied automatically at startup.
Run tests/verification before deployment:
pytest -q
python scripts/verify_project.py
