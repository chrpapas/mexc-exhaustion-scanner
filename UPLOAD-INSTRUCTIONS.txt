MEXC Exhaustion Scanner + Multi-Slot Short Trader v1.2.8

Full deployment:
- Deploy the entire repository to the existing Render Blueprint/services.
- Scanner and trader continue sharing the same PostgreSQL database.
- No new migration is required for v1.2.8; migration 013 remains the latest schema migration.
- Scanner research path horizon now defaults to 336h / 14d. If Render has a manually overridden RESEARCH_PATH_HORIZON_HOURS, set it to 336.
- Live scanner/trader entry and exit rules are NOT changed by this build.
- Run `python -m app.research_analytics_now` in the scanner shell for the new research report.
- Run `python -m app.research_status` to inspect research-path catch-up progress.
- The report includes Standard exit horizons, High-Risk TP20+timeout sweeps, stop-survival, shadow entry scores, feature interactions and delayed-entry simulations.
- All research uses PostgreSQL-only stored data; no additional MEXC requests are made.
