MEXC Exhaustion Scanner + Multi-Slot Futures Trader v1.3.56

Deploy the entire project/ZIP together.

v1.3.56 PROMOTES TREND PERSISTENCE V2
- Daily-Core hard skip remains unchanged.
- Frozen Persistence V1 early branch remains unchanged.
- New Mature-Run Weak-Breakdown V1 branch hard-skips when:
  Daily Bull=true AND Continuation Core=false AND run-to-breakdown>=24h AND
  previous 1h momentum>0 AND lower_high_and_close=false AND structural_break_15m=false.
- V2 frozen 08 Sep 2026 08:29 CEST after the SOPH review. Forward validation starts strictly after this time.
- STANDARD + HIGH_RISK; 6 generic slots x 5%; 30% max exposure; TP5; catastrophic SL75; 1x cross; no timeout.

Required Render strategy changes:
Scanner: SUBSCRIBER_SIGNAL_STRATEGY=tp5_sl75_daily_core_persistence_skip_v2
Trader:  TRADER_EXECUTION_STRATEGY=tp5_sl75_daily_core_persistence_skip_v2

For a clean paper-trader forward cohort, set:
TRADER_PAPER_RUN_ID=tp5_sl75_daily_core_persistence_skip_v2
PAPER_STARTING_EQUITY_USDT=2000   # if you want the same clean $2,000 baseline

Migration 020_persistence_v2_trader_decisions.sql runs automatically.
Do not change sizing/TP/SL environment values for this promotion.

Verification:
pytest -q
python scripts/verify_project.py
python -m app.trader_status
python -m app.report_now
python -m app.research_analytics_now
